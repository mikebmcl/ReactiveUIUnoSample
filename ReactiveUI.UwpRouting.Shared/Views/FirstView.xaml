<Page
    x:Class="ReactiveUI.UwpRouting.Views.FirstView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReactiveUI.UwpRouting.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock Text="Hello, I'm first the view!" Margin="8" />
            <StackPanel Background="Red" Orientation="Horizontal">
                <TextBlock Text="Enter currency amount:" Margin="8" VerticalAlignment="Center" />
                <TextBox x:Name="EnteredAmountTextBox" Height="35" Margin="8" />
            </StackPanel>
            <!--<TextBlock Text="{x:Bind ViewModel.UrlPathSegment, Mode=OneWay}" />-->
        </StackPanel>
        <Button Grid.Row="1"
                x:Name="NextPageButton"
                Content="Next"
                Margin="4" />
    </Grid>
</Page>
