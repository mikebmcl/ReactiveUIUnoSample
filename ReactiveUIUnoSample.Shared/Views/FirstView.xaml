<Page
    x:Class="ReactiveUIUnoSample.Views.FirstView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReactiveUIUnoSample.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                x:Name="NextPageButton"
                Content="Next"
                Margin="4" />
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
            <TextBlock Text="Hello, I'm first the view!" Margin="8" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Enter currency amount:" Margin="8" VerticalAlignment="Center" />
                <TextBox x:Name="EnteredAmountTextBox" Height="35" Margin="8" />
            </StackPanel>
            <!--<TextBlock Text="{x:Bind ViewModel.UrlPathSegment, Mode=OneWay}" />-->
        </StackPanel>
    </Grid>
</Page>
